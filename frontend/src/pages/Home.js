import { LandingPage } from "./Landing";
import {
  Stethoscope,
  Calendar,
  FileText
} from 'lucide-react';

const Home = () => {
    return (
        <main className="flex-grow flex flex-col justify-center">
            < LandingPage />
        </main>
    );
}
export { Home }